enum GameType {
  roshambo,
  penaltyCard,
  guessMaster,
  luckyJudge,
  glorySpin,
}

enum Round { first, second, third, forth, fifth }

enum RoshamboChoices { rock, paper, scissor, unknown }

enum PenaltyCardChoices { left, right, unknown }

enum GuessMasterChoices { one, two, three, four, five, unknown }

enum AllGameType {
  choice1,
  choice2,
  choice3,
  choice4,
  choice5,
  choice6,
}

enum PlayerType {
  playerOne,
  playerTwo,
  playerOthers,
  noOne,
}
